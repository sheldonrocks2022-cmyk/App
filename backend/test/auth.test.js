import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs/promises";import os from "node:os";import path from "node:path";import {MemberStore} from "../src/services/memberStore.js";
test("ESN account registration and password authentication",async()=>{const dir=await fs.mkdtemp(path.join(os.tmpdir(),"esn-"));const store=new MemberStore(path.join(dir,"members.json"));const m=await store.register({email:"Member@Example.com",password:"correct-horse-123",name:"ESN Member"});assert.match(m.memberId,/^esn_/);assert.equal((await store.authenticate({email:"member@example.com",password:"correct-horse-123"})).memberId,m.memberId);assert.equal(await store.authenticate({email:"member@example.com",password:"wrong-password"}),null);});
